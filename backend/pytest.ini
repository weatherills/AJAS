[pytest]
testpaths = tests
pythonpath = .
junit_family = xunit2
markers =
    emulator: talks to Azurite or a Cosmos emulator when available
# Coverage gates (also in .github/workflows/ci.yml):
#   app: --cov-fail-under=40
#   app/storage (data plane): --cov-fail-under=80

