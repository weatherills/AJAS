"""Review & Decision UI (backend) — Runbook Phase 4.

Approve/reject decisions with comments; surface match summary + suggestion.
Backend decision endpoints back the frontend review flow.

Placeholder blueprint — no routes are defined yet.
"""
import azure.functions as func

bp = func.Blueprint()
