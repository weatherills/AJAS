# Agent instructions

## Git

- Always work on `main`.
- Do not create feature branches, including `cursor/*` branches.
- Commit and push directly to `main`.
- Do not open pull requests unless the user explicitly asks.
