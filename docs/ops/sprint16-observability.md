# Sprint 16 observability

Use USE metrics (utilization, saturation, errors) and remaining error-budget alerts.
Propagate trace IDs ingest→apply and open `GET /api/v1/s16/traces`.
Health v5 adds a build SHA alongside the dependency matrix.
