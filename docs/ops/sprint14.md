# Sprint 14 operations

Sprint 14 adds more fixture-only adapters, matching/explanation polish,
privacy/ops tooling, and docs on top of Sprint 13. Optional adapters stay
**off**. Captcha paths never bypass. Robots/consent fail closed.

Batch gates: [sprint14-batches.md](./sprint14-batches.md).
Kanban: [sprint14-kanban.md](./sprint14-kanban.md).

Health `version` is `sprint14`. HTTP: `GET /api/v1/s14/status`, `GET /api/v1/s14/health`,
`GET /api/v1/s14/search`, `GET /api/v1/s14/traces`.
