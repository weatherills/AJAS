# Sprint 15 observability

Use RED metrics (rate, errors, duration) and SLO burn alerts.
Propagate trace IDs ingest→apply and open `GET /api/v1/s15/traces`.
Health v4 adds a build SHA alongside the dependency matrix.
