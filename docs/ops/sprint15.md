# Sprint 15 operations

Sprint 15 adds more fixture-only job boards, parse/matching polish,
mail/ops tooling, and docs on top of Sprint 14. Optional adapters stay
**off**. Captcha paths never bypass. Robots/consent fail closed.

Batch gates: [sprint15-batches.md](./sprint15-batches.md).
Kanban: [sprint15-kanban.md](./sprint15-kanban.md).

Health `version` is `sprint15`. HTTP: `GET /api/v1/s15/status`, `GET /api/v1/s15/health`,
`GET /api/v1/s15/search`, `GET /api/v1/s15/traces`, `GET /api/v1/s15/kanban`.
