# Sprint 16 operations

Sprint 16 adds more fixture-only job boards, parse/matching polish,
mail/ops tooling, and docs on top of Sprint 15. Optional adapters stay
**off**. Captcha paths never bypass. Robots/consent fail closed.

Batch gates: [sprint16-batches.md](./sprint16-batches.md).
Kanban: [sprint16-kanban.md](./sprint16-kanban.md).

Health `version` is `sprint16`. HTTP: `GET /api/v1/s16/status`, `GET /api/v1/s16/health`,
`GET /api/v1/s16/search`, `GET /api/v1/s16/traces`.
